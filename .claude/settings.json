{
  "permissions": {
    "allow": [
      "Skill(play-baba)",
      "Bash(start-baba)",
      "Bash(baba:*)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | { read file_path; if echo \"$file_path\" | grep -q '\\.rs$'; then cargo fmt --quiet; fi; }"
          }
        ]
      }
    ]
  }
}
